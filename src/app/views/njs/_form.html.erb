<%= form_with(model: nj, local: true) do |form| %>
  <% if nj.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nj.errors.count, "error") %> prohibited this nj from being saved:</h2>

      <ul>
        <% nj.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :verified %>
    <%= form.check_box :verified %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
